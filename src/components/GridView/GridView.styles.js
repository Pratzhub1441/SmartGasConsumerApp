import { StyleSheet } from "react-native";
import colors from '../../assets/colors/colors';

const styles= StyleSheet.create({
    Container: {
        height: 122,
    },
    header: {
        flexDirection: "row",
        backgroundColor: "#ABCDD1",
        paddingVertical: 10,
        width: "100%",
    },
});

export default styles;